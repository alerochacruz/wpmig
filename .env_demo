# shellcheck disable=SC2034
# =============================================================================
# WPMIG - Ejemplo de configuración de entorno
# Copia este archivo a ".env" y ajusta los valores según sea necesario.
# Cualquier variable no definida se solicitará de forma interactiva.
# =============================================================================

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DEL SERVIDOR DE ORIGEN
# -----------------------------------------------------------------------------

# Nombre de host o dirección IP del servidor de origen
SOURCE_HOST=192.168.0.250

# Puerto SSH (por defecto: 22)
SOURCE_PORT=22

# Usuario SSH
SOURCE_USER=root

# Método de autenticación:
#   1 = Llave SSH (recomendado)
#   2 = Contraseña
SOURCE_AUTH_METHOD=1

# Ruta a la llave privada SSH (usada si AUTH_METHOD=1)
SOURCE_KEY_PATH=./demo_keys/demo_ed25519

# Contraseña SSH (usada si AUTH_METHOD=2 o si falla la llave)
# SOURCE_PASSWORD=tucontraseña

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DEL SERVIDOR DE DESTINO
# -----------------------------------------------------------------------------

# Nombre de host o dirección IP del servidor de destino
DESTINATION_HOST=192.168.0.251

# Puerto SSH (por defecto: 22)
DESTINATION_PORT=22

# Usuario SSH
DESTINATION_USER=root

# Método de autenticación:
#   1 = Llave SSH
#   2 = Contraseña
DESTINATION_AUTH_METHOD=1

# Ruta a la llave privada SSH (usada si AUTH_METHOD=1)
DESTINATION_KEY_PATH=./demo_keys/demo_ed25519

# Contraseña SSH (usada si AUTH_METHOD=2 o si falla la llave)
# DESTINATION_PASSWORD=tucontraseña

# -----------------------------------------------------------------------------
# CONFIGURACIONES ADICIONALES
# -----------------------------------------------------------------------------

# Credenciales de base de datos
DESTINATION_DB_NAME=exodus_wpdb
DESTINATION_DB_USER=exodus_wpdbuser
DESTINATION_DB_PASS=exodus_wpdbpass
DESTINATION_DB_HOST=localhost

# Ruta de WordPress en el servidor de origen
SOURCE_WP_PATH=/var/www/html

# Ruta de WordPress en el servidor de destino
DESTINATION_WP_PATH=/var/www/html

# Usuario del servidor web en el destino
DESTINATION_WEB_USER=www-data

# URLs antigua y nueva del sitio
OLD_URL=http://192.168.0.250
NEW_URL=http://192.168.0.251
